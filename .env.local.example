# Google OAuth (sign-in)
# Create OAuth 2.0 credentials at https://console.cloud.google.com/apis/credentials
# Add authorized redirect URI: http://localhost:3000/api/auth/callback (dev) and https://your-domain.com/api/auth/callback (prod)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Session signing (min 32 chars, use: openssl rand -base64 32)
AUTH_SECRET=your_auth_secret_key_min_32_chars

# Cloudflare R2
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_key_id
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=style-layer-assets
R2_PUBLIC_DOMAIN=https://your-pub-link.com

# Jimeng AI 4.0 (即梦) - Volcano Visual API
# 需使用火山引擎控制台创建的 Access Key / Secret Key
JIMENG_ACCESS_KEY=your_access_key
JIMENG_SECRET_KEY=your_secret_key

# Database (optional for MVP)
DATABASE_ID=your_d1_db_id